// En un archivo separado, por ejemplo, types.ts
export interface Post {
    id: string; 
    title: string;
    message: string;
    creation_date: string;
    postname: string;
    user_id: string;
   }
  